// ACTIONS
import alt from '../alt.js';

class Actions {
  searchByKeyword(data) {
    return data;
  }

  searchByTitle(data) {
    return data;
  }

  searchByAuthor(data) {
    return data;
  }
}

export default alt.createActions(Actions);
