const initialState = {
  searchResults: {},
  searchQuery: '',
  searchField: '',
  sort: {
    sortFilter: 'title',
    sortOrder: 'asc',
  },
  workDetail: { instances: [] },
};

export default initialState;
